/**
 * Changes to refactor:
 * 		- All functions (templated or otherwise) converted to keyword-argument types.
 *      - Categorize all types/functions/members into dispatch.
 *      First: list, types, dispatch, object, containers, type_traits, arg_lists.
 */